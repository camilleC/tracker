# tracker
A backend API project to practice building Go services with structured logging, middleware, context propagation, and graceful shutdown. Includes endpoints for creating and retrieving data, with optional aggregation.
